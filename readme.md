# Лабораторные работы по информатике

## Компиляция:
              < pathToGCC > -o run < pathToFile >

### Пример:
              gcc -o run .\src\3\Smirnov_if_1.c

## Запуск:
            run.exe
